<<<<<<< HEAD
# You can override the included template(s) by including variable overrides
# SAST customization: https://docs.gitlab.com/user/application_security/sast/#available-cicd-variables
# Secret Detection customization: https://docs.gitlab.com/user/application_security/secret_detection/pipeline/configure/
# Dependency Scanning customization: https://docs.gitlab.com/user/application_security/dependency_scanning/#customizing-analyzer-behavior
# Container Scanning customization: https://docs.gitlab.com/user/application_security/container_scanning/#customizing-analyzer-behavior
# Note that environment variables can be set in several places
# See https://docs.gitlab.com/ci/variables/#cicd-variable-precedence
stages:
- build
- test
- deploy
- review
- dast
- staging
- canary
- production
- incremental rollout 10%
- incremental rollout 25%
- incremental rollout 50%
- incremental rollout 100%
- performance
- cleanup
- secret-detection
variables:
  SECRET_DETECTION_ENABLED: 'true'
secret_detection:
  stage: secret-detection
include:
- template: Auto-DevOps.gitlab-ci.yml
=======
stages:
  - build
  - release

workflow:
  rules:
    - if: $CI_COMMIT_TAG
    - when: never

default:
  tags:
    - windows

build_launcher:
  stage: build
  script:
    - powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Build-Launcher.ps1 -ProjectPath "Wow-Launcher\Wow-Launcher.csproj" -Configuration "Release" -OutputDirectory "artifacts"
  artifacts:
    paths:
      - artifacts/Wow-Launcher.exe
    expire_in: never

release_launcher:
  stage: release
  needs:
    - job: build_launcher
      artifacts: true
  script:
    - powershell -NoProfile -ExecutionPolicy Bypass -Command "New-Item -ItemType Directory -Force -Path release | Out-Null; Copy-Item artifacts\Wow-Launcher.exe release\Wow-Launcher.exe -Force"
    - powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Publish-GitLabRelease.ps1 -AssetPath "release\Wow-Launcher.exe" -AssetName "Wow-Launcher.exe"
  artifacts:
    paths:
      - release/Wow-Launcher.exe
    expire_in: never
>>>>>>> 08759a6 (Initial launcher with GitLab self-update support)
